comprimento = len(tupla)   #quantidade lista
existe = elemento in tupla  #verificando se existe tal elemento
ocorrencias = tupla.count(elemento) #contando a ocorrencia de um elemento
indice = tupla.index(elemento)   #encontrando um indice
maior = max(tupla) #min
menor = min(tupla) #max
lista = list(tupla)    #convertendo lista em uma tupla
tupla = tuple(lista)   #vice versa



